# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Test / type / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Local editor settings (machine-specific interpreter paths)
.vscode/

# Local scratch
.tmp*
*.tmp
src.zip
tests.zip
*.zip

# Re-downloadable datasets at repo root only (keep local copies; do not delete).
# Anchored so src/nn_compression/models/ is not ignored.
/data/

# Training checkpoints at repo root only (keep local copies; do not delete)

# Secrets / credentials
.env
.env.*
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json

# Obsidian local workspace state
docs/.obsidian/workspace.json
docs/.obsidian/workspace-mobile.json

# Research records (CSV / figures) stay tracked. Do not ignore results/.
